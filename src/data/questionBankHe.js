// TEMPORARY placeholder so the app builds while the full Hebrew translation
// is being generated. This re-uses the English bank and is replaced by real
// Hebrew content. Do not ship Hebrew mode to users until this is replaced.
import questionBank from "./questionBank";

const questionBankHe = questionBank;

export default questionBankHe;
